# Normalize line endings to LF in the repo
* text=auto eol=lf

# Binary assets — never touch line endings, don't diff
*.png binary
*.jpg binary
*.jpeg binary
*.mp4 binary
*.webm binary
*.woff2 binary
